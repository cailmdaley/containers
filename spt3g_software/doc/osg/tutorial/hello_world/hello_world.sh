#!/usr/bin/env bash

# Printing environment for debugging
env

# Debugging and to see where we ended up
echo "HOSTNAME: $HOSTNAME"

# Saying Hi!
echo "Hello World"

# Just to show which directory we ended up on
echo $PWD

# And what is in the directory
ls -l $PWD